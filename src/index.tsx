import * as React from 'react';
import * as ReactDOM from 'react-dom';
import "./style.css";

import { Runner } from "./components/runner";

const rootEl = document.getElementById('root');
ReactDOM.render(<Runner />, rootEl);
